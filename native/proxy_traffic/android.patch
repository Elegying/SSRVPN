diff --git a/adapter/outbound/base.go b/adapter/outbound/base.go
index bbac4e80..66cc5a47 100644
--- a/adapter/outbound/base.go
+++ b/adapter/outbound/base.go
@@ -223,6 +223,7 @@ func (b *BasicOption) NewTunnel() C.Tunnel {
 }
 
 type conn struct {
+	proxy bool
 	N.ExtendedConn
 	chain       C.Chain
 	pdChain     C.Chain
@@ -278,12 +279,13 @@ func NewConn(c net.Conn, a C.ProxyAdapter) C.Conn {
 	if _, ok := c.(syscall.Conn); !ok { // exclusion system conn like *net.TCPConn
 		c = N.NewDeadlineConn(c) // most conn from outbound can't handle readDeadline correctly
 	}
-	cc := &conn{N.NewExtendedConn(c), nil, nil, a.Addr()}
+	cc := &conn{isProxyOutbound(a.Type()), N.NewExtendedConn(c), nil, nil, a.Addr()}
 	cc.AppendToChains(a)
 	return cc
 }
 
 type packetConn struct {
+	proxy bool
 	N.EnhancePacketConn
 	chain       C.Chain
 	pdChain     C.Chain
@@ -344,7 +346,7 @@ func newPacketConn(pc net.PacketConn, a ProxyAdapter) C.PacketConn {
 	if _, ok := pc.(syscall.Conn); !ok { // exclusion system conn like *net.UDPConn
 		epc = N.NewDeadlineEnhancePacketConn(epc) // most conn from outbound can't handle readDeadline correctly
 	}
-	cpc := &packetConn{epc, nil, nil, a.Name(), utils.NewUUIDV4().String(), a.Addr(), a.ResolveUDP}
+	cpc := &packetConn{isProxyOutbound(a.Type()), epc, nil, nil, a.Name(), utils.NewUUIDV4().String(), a.Addr(), a.ResolveUDP}
 	cpc.AppendToChains(a)
 	return cpc
 }
@@ -398,3 +400,17 @@ func NewAutoCloseProxyAdapter(adapter ProxyAdapter) ProxyAdapter {
 	runtime.SetFinalizer(proxy, (*autoCloseProxyAdapter).Close)
 	return proxy
 }
+
+// Classification is captured at the actual outbound, independent of node names
+// and subsequent selector changes. Groups preserve the underlying connection.
+func isProxyOutbound(kind C.AdapterType) bool {
+	switch kind {
+	case C.Direct, C.Reject, C.RejectDrop, C.Compatible, C.Pass, C.PassRule, C.Dns:
+		return false
+	default:
+		return true
+	}
+}
+
+func (c *conn) IsProxy() bool       { return c.proxy }
+func (c *packetConn) IsProxy() bool { return c.proxy }
diff --git a/component/resolver/relay.go b/component/resolver/relay.go
index ca7b32b9..7d420235 100644
--- a/component/resolver/relay.go
+++ b/component/resolver/relay.go
@@ -94,7 +94,17 @@ func relayDnsPacket(ctx context.Context, payload []byte, target []byte, maxSize
 		r.Truncate(maxSize)
 	}
 	r.Compress = true
-	return r.PackBuffer(target)
+	data, err := r.PackBuffer(target)
+	if err != nil {
+		return nil, err
+	}
+	// PackBuffer sizes its scratch space by the *uncompressed* message length and may
+	// have allocated a new slice even though the compressed result fits into target.
+	// Callers (e.g. the tun dns hijack) assume the result is backed by target, so copy it back.
+	if len(data) > 0 && len(data) <= len(target) && &data[0] != &target[0] {
+		data = target[:copy(target, data)]
+	}
+	return data, nil
 }
 
 // RelayDnsPacket will truncate udp message up to SafeDnsPacketSize
diff --git a/config/config.go b/config/config.go
index 9cb1acf8..c0363b2b 100644
--- a/config/config.go
+++ b/config/config.go
@@ -1648,7 +1648,16 @@ func parseAuthentication(rawRecords []string) []auth.AuthUser {
 }
 
 func parseIPV6(rawCfg *RawConfig) {
-	if !rawCfg.IPv6 || !verifyIP6() {
+	if !rawCfg.IPv6 {
+		rawCfg.DNS.FakeIPRange6 = ""
+		return
+	}
+	// An explicit capture address is independent of physical IPv6 availability.
+	// IPv6 targets can be carried over an IPv4 connection to a proxy server.
+	if len(rawCfg.Tun.Inet6Address) > 0 {
+		return
+	}
+	if !verifyIP6() {
 		rawCfg.DNS.FakeIPRange6 = ""
 		rawCfg.Tun.Inet6Address = nil
 	}
diff --git a/constant/adapters.go b/constant/adapters.go
index 37d8af66..42afbc1e 100644
--- a/constant/adapters.go
+++ b/constant/adapters.go
@@ -64,6 +64,8 @@ const (
 var ErrNotSupport = errors.New("no support")
 
 type Connection interface {
+	// IsProxy records the actual outbound type before groups append their names.
+	IsProxy() bool
 	Chains() Chain
 	ProviderChains() Chain
 	AppendToChains(adapter ProxyAdapter)
diff --git a/hub/route/server.go b/hub/route/server.go
index 7b8562ca..4fcee166 100644
--- a/hub/route/server.go
+++ b/hub/route/server.go
@@ -121,6 +121,7 @@ func router(isDebug bool, secret string, dohServer string, cors Cors) *chi.Mux {
 		r.Get("/", hello)
 		r.Get("/logs", getLogs)
 		r.Get("/traffic", traffic)
+		r.Get("/ssrvpn/traffic", ssrvpnTraffic)
 		r.Get("/memory", memory)
 		r.Get("/version", version)
 		r.Mount("/configs", configRouter())
diff --git a/tunnel/statistic/tracker.go b/tunnel/statistic/tracker.go
index 2fbb1cf9..f336d04e 100644
--- a/tunnel/statistic/tracker.go
+++ b/tunnel/statistic/tracker.go
@@ -34,7 +34,8 @@ type TrackerInfo struct {
 }
 
 type tcpTracker struct {
-	C.Conn `json:"-"`
+	proxySession *proxyTrafficSession
+	C.Conn       `json:"-"`
 	*TrackerInfo
 	manager *Manager
 
@@ -54,6 +55,9 @@ func (tt *tcpTracker) Read(b []byte) (int, error) {
 	download := int64(n)
 	if tt.pushToManager {
 		tt.manager.PushDownloaded(download)
+		if tt.proxySession != nil {
+			tt.proxySession.download.Add(download)
+		}
 	}
 	tt.DownloadTotal.Add(download)
 	return n, err
@@ -64,6 +68,9 @@ func (tt *tcpTracker) ReadBuffer(buffer *buf.Buffer) (err error) {
 	download := int64(buffer.Len())
 	if tt.pushToManager {
 		tt.manager.PushDownloaded(download)
+		if tt.proxySession != nil {
+			tt.proxySession.download.Add(download)
+		}
 	}
 	tt.DownloadTotal.Add(download)
 	return
@@ -73,6 +80,9 @@ func (tt *tcpTracker) UnwrapReader() (io.Reader, []N.CountFunc) {
 	return tt.Conn, []N.CountFunc{func(download int64) {
 		if tt.pushToManager {
 			tt.manager.PushDownloaded(download)
+			if tt.proxySession != nil {
+				tt.proxySession.download.Add(download)
+			}
 		}
 		tt.DownloadTotal.Add(download)
 	}}
@@ -83,6 +93,9 @@ func (tt *tcpTracker) Write(b []byte) (int, error) {
 	upload := int64(n)
 	if tt.pushToManager {
 		tt.manager.PushUploaded(upload)
+		if tt.proxySession != nil {
+			tt.proxySession.upload.Add(upload)
+		}
 	}
 	tt.UploadTotal.Add(upload)
 	return n, err
@@ -93,6 +106,9 @@ func (tt *tcpTracker) WriteBuffer(buffer *buf.Buffer) (err error) {
 	err = tt.Conn.WriteBuffer(buffer)
 	if tt.pushToManager {
 		tt.manager.PushUploaded(upload)
+		if tt.proxySession != nil {
+			tt.proxySession.upload.Add(upload)
+		}
 	}
 	tt.UploadTotal.Add(upload)
 	return
@@ -102,6 +118,9 @@ func (tt *tcpTracker) UnwrapWriter() (io.Writer, []N.CountFunc) {
 	return tt.Conn, []N.CountFunc{func(upload int64) {
 		if tt.pushToManager {
 			tt.manager.PushUploaded(upload)
+			if tt.proxySession != nil {
+				tt.proxySession.upload.Add(upload)
+			}
 		}
 		tt.UploadTotal.Add(upload)
 	}}
@@ -116,12 +135,13 @@ func (tt *tcpTracker) Upstream() any {
 	return tt.Conn
 }
 
-func NewTCPTracker(conn C.Conn, manager *Manager, metadata *C.Metadata, rule C.Rule, uploadTotal int64, downloadTotal int64, pushToManager bool) *tcpTracker {
+func NewTCPTracker(conn C.Conn, manager *Manager, metadata *C.Metadata, rule C.Rule, uploadTotal int64, downloadTotal int64, pushToManager bool, sessions ...*proxyTrafficSession) *tcpTracker {
 	metadata.RemoteDst = conn.RemoteDestination()
 
 	t := &tcpTracker{
-		Conn:    conn,
-		manager: manager,
+		proxySession: proxySessionFor(conn, pushToManager, sessions),
+		Conn:         conn,
+		manager:      manager,
 		TrackerInfo: &TrackerInfo{
 			UUID:          utils.NewUUIDV4(),
 			Start:         time.Now(),
@@ -138,9 +158,15 @@ func NewTCPTracker(conn C.Conn, manager *Manager, metadata *C.Metadata, rule C.R
 	if pushToManager {
 		if uploadTotal > 0 {
 			manager.PushUploaded(uploadTotal)
+			if t.proxySession != nil {
+				t.proxySession.upload.Add(uploadTotal)
+			}
 		}
 		if downloadTotal > 0 {
 			manager.PushDownloaded(downloadTotal)
+			if t.proxySession != nil {
+				t.proxySession.download.Add(downloadTotal)
+			}
 		}
 	}
 
@@ -154,6 +180,7 @@ func NewTCPTracker(conn C.Conn, manager *Manager, metadata *C.Metadata, rule C.R
 }
 
 type udpTracker struct {
+	proxySession *proxyTrafficSession
 	C.PacketConn `json:"-"`
 	*TrackerInfo
 	manager *Manager
@@ -174,6 +201,9 @@ func (ut *udpTracker) ReadFrom(b []byte) (int, net.Addr, error) {
 	download := int64(n)
 	if ut.pushToManager {
 		ut.manager.PushDownloaded(download)
+		if ut.proxySession != nil {
+			ut.proxySession.download.Add(download)
+		}
 	}
 	ut.DownloadTotal.Add(download)
 	return n, addr, err
@@ -184,6 +214,9 @@ func (ut *udpTracker) WaitReadFrom() (data []byte, put func(), addr net.Addr, er
 	download := int64(len(data))
 	if ut.pushToManager {
 		ut.manager.PushDownloaded(download)
+		if ut.proxySession != nil {
+			ut.proxySession.download.Add(download)
+		}
 	}
 	ut.DownloadTotal.Add(download)
 	return
@@ -194,6 +227,9 @@ func (ut *udpTracker) WriteTo(b []byte, addr net.Addr) (int, error) {
 	upload := int64(n)
 	if ut.pushToManager {
 		ut.manager.PushUploaded(upload)
+		if ut.proxySession != nil {
+			ut.proxySession.upload.Add(upload)
+		}
 	}
 	ut.UploadTotal.Add(upload)
 	return n, err
@@ -208,12 +244,13 @@ func (ut *udpTracker) Upstream() any {
 	return ut.PacketConn
 }
 
-func NewUDPTracker(conn C.PacketConn, manager *Manager, metadata *C.Metadata, rule C.Rule, uploadTotal int64, downloadTotal int64, pushToManager bool) *udpTracker {
+func NewUDPTracker(conn C.PacketConn, manager *Manager, metadata *C.Metadata, rule C.Rule, uploadTotal int64, downloadTotal int64, pushToManager bool, sessions ...*proxyTrafficSession) *udpTracker {
 	metadata.RemoteDst = conn.RemoteDestination()
 
 	ut := &udpTracker{
-		PacketConn: conn,
-		manager:    manager,
+		proxySession: proxySessionFor(conn, pushToManager, sessions),
+		PacketConn:   conn,
+		manager:      manager,
 		TrackerInfo: &TrackerInfo{
 			UUID:          utils.NewUUIDV4(),
 			Start:         time.Now(),
@@ -230,9 +267,15 @@ func NewUDPTracker(conn C.PacketConn, manager *Manager, metadata *C.Metadata, ru
 	if pushToManager {
 		if uploadTotal > 0 {
 			manager.PushUploaded(uploadTotal)
+			if ut.proxySession != nil {
+				ut.proxySession.upload.Add(uploadTotal)
+			}
 		}
 		if downloadTotal > 0 {
 			manager.PushDownloaded(downloadTotal)
+			if ut.proxySession != nil {
+				ut.proxySession.download.Add(downloadTotal)
+			}
 		}
 	}
 
diff --git a/tunnel/tunnel.go b/tunnel/tunnel.go
index 470f7299..add1972c 100644
--- a/tunnel/tunnel.go
+++ b/tunnel/tunnel.go
@@ -416,6 +416,7 @@ func processUDP(queue chan C.PacketAdapter) {
 }
 
 func handleUDPConn(packet C.PacketAdapter) {
+	proxySession := statistic.CaptureProxyTrafficSession()
 	if !isHandle(packet.Metadata().Type) {
 		packet.Drop()
 		return
@@ -464,17 +465,22 @@ func handleUDPConn(packet C.PacketAdapter) {
 			dialMetadata := metadata.Pure()
 			ctx, cancel := context.WithTimeout(context.Background(), C.DefaultUDPTimeout)
 			defer cancel()
+			candidates := ssrvpnProxyTargetCandidates(ctx, metadata, proxy)
+			attempt := 0
 			rawPc, err := retry(ctx, func(ctx context.Context) (C.PacketConn, error) {
+				dialMetadata = ssrvpnTargetAt(candidates, attempt).Pure()
+				attempt++
 				return proxy.ListenPacketContext(ctx, dialMetadata)
 			}, func(err error) {
 				logMetadataErr(metadata, rule, proxy, err)
 			})
 			if err != nil {
+				ssrvpnLogIPv6TargetFailure(candidates, attempt, proxySession.RecordIPv6TargetFailure)
 				return nil, nil, err
 			}
 			logMetadata(metadata, rule, rawPc)
 
-			pc := statistic.NewUDPTracker(rawPc, statistic.DefaultManager, metadata, rule, 0, 0, true)
+			pc := statistic.NewUDPTracker(rawPc, statistic.DefaultManager, metadata, rule, 0, 0, true, proxySession)
 
 			sender.AddMapping(originMetadata, dialMetadata)
 			oAddrPort := dialMetadata.AddrPort()
@@ -498,6 +504,7 @@ func handleUDPConn(packet C.PacketAdapter) {
 }
 
 func handleTCPConn(connCtx C.ConnContext) {
+	proxySession := statistic.CaptureProxyTrafficSession()
 	if !isHandle(connCtx.Metadata().Type) {
 		_ = connCtx.Conn().Close()
 		return
@@ -571,7 +578,11 @@ func handleTCPConn(connCtx C.ConnContext) {
 
 	ctx, cancel := context.WithTimeout(context.Background(), C.DefaultTCPTimeout)
 	defer cancel()
+	candidates := ssrvpnProxyTargetCandidates(ctx, metadata, proxy)
+	attempt := 0
 	remoteConn, err := retry(ctx, func(ctx context.Context) (remoteConn C.Conn, err error) {
+		dialMetadata = ssrvpnTargetAt(candidates, attempt)
+		attempt++
 		remoteConn, err = proxy.DialContext(ctx, dialMetadata)
 		if err != nil {
 			return
@@ -593,7 +604,7 @@ func handleTCPConn(connCtx C.ConnContext) {
 			peekMutex.Lock()
 			defer peekMutex.Unlock()
 			peekBytes, _ = conn.Peek(conn.Buffered())
-			_, err = remoteConn.Write(peekBytes)
+			err = ssrvpnWriteInitialPayload(remoteConn, peekBytes)
 			if err != nil {
 				return
 			}
@@ -606,11 +617,13 @@ func handleTCPConn(connCtx C.ConnContext) {
 		logMetadataErr(metadata, rule, proxy, err)
 	})
 	if err != nil {
+		ssrvpnLogIPv6TargetFailure(candidates, attempt, proxySession.RecordIPv6TargetFailure)
 		return
 	}
 	logMetadata(metadata, rule, remoteConn)
 
-	remoteConn = statistic.NewTCPTracker(remoteConn, statistic.DefaultManager, metadata, rule, int64(peekLen), 0, true)
+	remoteConn = ssrvpnObserveWorkingFamily(remoteConn, metadata, dialMetadata, proxy)
+	remoteConn = statistic.NewTCPTracker(remoteConn, statistic.DefaultManager, metadata, rule, int64(peekLen), 0, true, proxySession)
 	defer func(remoteConn C.Conn) {
 		_ = remoteConn.Close()
 	}(remoteConn)
@@ -696,6 +709,10 @@ func getRules(metadata *C.Metadata) []C.Rule {
 }
 
 func shouldStopRetry(err error) bool {
+	var payloadWrite *ssrvpnPayloadWriteError
+	if errors.As(err, &payloadWrite) {
+		return true
+	}
 	if errors.Is(err, resolver.ErrIPNotFound) {
 		return true
 	}
diff --git a/dns/resolver.go b/dns/resolver.go
index 73a22997..d2482af2 100644
--- a/dns/resolver.go
+++ b/dns/resolver.go
@@ -74,36 +74,7 @@ func (r *Resolver) LookupIPPrimaryIPv4(ctx context.Context, host string) (ips []
 }
 
 func (r *Resolver) LookupIP(ctx context.Context, host string) (ips []netip.Addr, err error) {
-	ch := make(chan []netip.Addr, 1)
-	go func() {
-		defer close(ch)
-		ip, err := r.lookupIP(ctx, host, D.TypeAAAA)
-		if err != nil {
-			return
-		}
-
-		ch <- ip
-	}()
-
-	ips, err = r.lookupIP(ctx, host, D.TypeA)
-	var waitIPv6 *time.Timer
-	if r != nil && r.ipv6Timeout > 0 {
-		waitIPv6 = time.NewTimer(r.ipv6Timeout)
-	} else {
-		waitIPv6 = time.NewTimer(100 * time.Millisecond)
-	}
-	defer waitIPv6.Stop()
-	select {
-	case ipv6s, open := <-ch:
-		if !open && err != nil {
-			return nil, resolver.ErrIPNotFound
-		}
-		ips = append(ips, ipv6s...)
-	case <-waitIPv6.C:
-		// wait ipv6 result
-	}
-
-	return ips, nil
+	return r.ssrvpnLookupIP(ctx, host)
 }
 
 // LookupIPv4 request with TypeA
diff --git a/adapter/outbound/direct.go b/adapter/outbound/direct.go
index 5741bf3f..65f82bc0 100644
--- a/adapter/outbound/direct.go
+++ b/adapter/outbound/direct.go
@@ -3,6 +3,7 @@ package outbound
 import (
 	"context"
 	"fmt"
+	"net/netip"
 
 	"github.com/metacubex/mihomo/component/dialer"
 	"github.com/metacubex/mihomo/component/loopback"
@@ -28,6 +29,14 @@ func (d *Direct) DialContext(ctx context.Context, metadata *C.Metadata) (C.Conn,
 	opts := d.DialOptions()
-	opts = append(opts, dialer.WithResolver(resolver.DirectHostResolver))
+	opts = append(opts, dialer.WithResolver(d.ssrvpnDirectResolver()))
 	c, err := dialer.DialContext(ctx, "tcp", metadata.RemoteAddress(), opts...)
+	if target := ssrvpnDirectFamilyFallback(ctx, metadata, err, func(ctx context.Context, host string) ([]netip.Addr, error) {
+		return resolver.LookupIPWithResolver(ctx, host, resolver.DirectHostResolver)
+	}); target != nil {
+		if checkErr := d.loopBack.CheckConn(target); checkErr != nil {
+			return nil, checkErr
+		}
+		c, err = dialer.DialContext(ctx, "tcp", target.RemoteAddress(), opts...)
+	}
 	if err != nil {
 		return nil, err
 	}
@@ -57,7 +66,7 @@ func (d *Direct) ResolveUDP(ctx context.Context, metadata *C.Metadata) error {
 	if (!metadata.Resolved() || resolver.DirectHostResolver != resolver.DefaultResolver) && metadata.Host != "" {
-		ip, err := resolveIPWithResolver(ctx, metadata.Host, d.prefer, resolver.DirectHostResolver)
+		ip, err := resolveIPWithResolver(ctx, metadata.Host, d.prefer, d.ssrvpnDirectResolver())
 		if err != nil {
 			return fmt.Errorf("can't resolve ip: %w", err)
 		}
 		metadata.DstIP = ip
 	}
diff --git a/hub/executor/executor.go b/hub/executor/executor.go
index 8e0d09e6..d578eba6 100644
--- a/hub/executor/executor.go
+++ b/hub/executor/executor.go
@@ -105,7 +105,6 @@ func ApplyConfig(cfg *config.Config, force bool) {
 	updateNTP(cfg.NTP)
 	updateDNS(cfg.DNS, cfg.General.IPv6)
 	updateListeners(cfg.General, cfg.Listeners, force)
-	updateTun(cfg.General) // tun should not care "force"
 	updateIPTables(cfg)
 	updateTunnels(cfg.Tunnels)
 
@@ -115,6 +114,15 @@ func ApplyConfig(cfg *config.Config, force bool) {
 	loadProvider(cfg.Providers)
 	updateProfile(cfg)
 	loadProvider(cfg.RuleProviders)
+	// Commit capture only after core and rule preparation has finished.
+	if !cfg.General.Tun.Enable || ssrvpnTunRulesReady(cfg.RuleProviders) {
+		updateTun(cfg.General)
+		if cfg.General.Tun.Enable && listener.GetTunConf().Enable {
+			log.Infoln("SSRVPN_TUN_COMMITTED")
+		}
+	} else {
+		log.Errorln("SSRVPN: TUN capture withheld because routing rules are not ready")
+	}
 	runtime.GC()
 	tunnel.OnRunning()
 	updateUpdater(cfg)
