org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Third-party Flutter plugins still use the legacy Kotlin Gradle plugin. The app
# module opts into AGP 9 built-in Kotlin explicitly while they migrate upstream.
android.builtInKotlin=false
# Flutter 3.44 still requires the old AGP DSL compatibility mode.
android.newDsl=false
