name: ssrvpn_shared
description: Shared SSRVPN models and services.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.0.0
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.24.0
