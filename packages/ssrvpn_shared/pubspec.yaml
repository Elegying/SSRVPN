name: ssrvpn_shared
description: Shared SSRVPN models and services.
publish_to: 'none'
version: 1.0.0
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  country_flags: ^4.0.0
  flutter:
    sdk: flutter
  http: ^1.0.0
  uuid: '>=3.0.6 <5.0.0'
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.24.0
