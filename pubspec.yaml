name: ssrvpn_workspace
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'

workspace:
  - packages/ssrvpn_shared
  - SSRVPN_Android
  - SSRVPN_MacOS
  - SSRVPN_Windows

# Pin the upstream renderer with the documented capture-coordinate repair.
dependency_overrides:
  liquid_glass_widgets:
    path: packages/liquid_glass_widgets
