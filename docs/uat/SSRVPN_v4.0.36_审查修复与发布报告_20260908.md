# SSRVPN v4.0.36 审查修复与发布报告

维护者授权按上轮建议修复、再次审查并同步发版。本轮从正式 v4.0.35 源码
`1338d04174dca81384f03cee787773963b4a685f` 开始，候选版本 `4.0.36+4036`。
本文先记录可复核的源码与自动化证据；正式发布状态以精确标签、流水线和资产复核为准。

## 修复与优化

| 问题 | 本轮处理 |
| --- | --- |
| 部分国旗缺失，桌面缓存按名称可能串用旧国别 | 三端共用国旗提示解析与后台 IP 查询，按协议/地址/端口摘要缓存，真实 IP 国别优先；同名改地址失效，改名和重连复用 |
| Android 重载失败撤销意图后再校验，错误状态无法发布 | 先确认当前代际再发布失败并撤销意图，清除忙碌状态；用户主动取消优先 |
| macOS 更新前忽略停止错误，可能在清理完成前打开安装包 | 串行等待幂等停止与清理，失败或出现新连接意图时阻止安装入口 |
| Windows 特殊地理选择组可能停在旧节点 | 检测域名统一走 PROXY，删除重复组与失效查询服务 |
| 大订阅在 UI isolate 重复提取、校验、构建运行文本 | 扩展现有可取消短命 worker，合并结果携带运行文本；保留小输入快路径和缓存事务回滚 |
| macOS 200 ms 守护轮询，Android 探测重复创建线程 | macOS 改为 1 秒可取消等待；Android 单个有界 worker，超时或中断不释放仍在运行的 JNI 占用 |
| Prepare 与 Release 重复重建相同核心 | main CI 保留规范源码重建；下游校验精确 main CI、任务、artifact 身份及摘要后复用，缺失/过期才回退 |
| 依赖更新混合固定 SDK 和构建工具链 | 增加前置锁文件解析检查，SDK 绑定包与 Android 工具链分别分组；未直接升级或合并旧不兼容 PR |
| 正式发布状态与操作手册滞后 | 补记正式 v4.0.35，发布入口统一到 Prepare Release；保留历史实机证据原始版本 |

## 国旗后台行为

- 名称、emoji、有效地区元数据用于首次提示。连接稳定 15 秒后，以最多两个并发任务解析
  节点服务器真实 IP，再通过随客户端打包的 GeoIP 数据库查国家；数据库解压与初始化放入后台 isolate。
- 域名解析通过当前本地 HTTP 代理访问 HTTPS DNS，不切换节点，不绑定物理网卡绕过代理，
  不把国别任务加入延迟测试链路；正在连接、测速、恢复或断开时取消请求。
- DNS 请求有总时间、响应大小、答案条数与 CNAME 关联约束；拒绝私网、保留地址及 fake-IP。
  国别解析不逐 IP 请求第三方 Geo 服务。结果表示节点服务器 IP 所在国家，不承诺等于代理出口国家。
- 成功结果持久缓存，未改变的端点后续无需再次查询；缓存文件只保存端点 SHA-256 和国别，
  不含 IP、节点名称、凭据或订阅地址。缓存有大小/数量上限，损坏或写入失败不影响连接。
- 失败保留已有提示，未知节点继续显示地球，不编造国家；失败不会永久缓存，在后续稳定会话可重试。
  取消或旧会话迟到结果不更新当前节点。

## 验证范围

| 验证范围 | 结果 |
| --- | --- |
| 共享完整测试 | 1114 项通过，覆盖率 87.90%，门槛 65% |
| Android 完整测试 | 293 项通过，覆盖率 70.51%，门槛 30% |
| macOS 完整测试 | 317 项通过、1 项首次超时；保留原 30 秒限制，单项复跑 9 秒通过 |
| macOS 覆盖率 | 总体 72.77%；生命周期 76.67%（底线 60%），系统代理 89.46%（底线 80%） |
| Windows Dart 完整测试 | 298 项通过、7 项仅 Windows API 用例按宿主跳过、1 项等待完成提示首次失败；单项复跑 8 秒通过 |
| Windows 覆盖率 | 总体 59.79%；生命周期 51.14%（底线 51%） |
| Android 原生 JVM | 37 个测试套件、178 项通过，零失败/跳过 |
| 国旗查询与缓存 | 查询 21 项；最终控制器 15 项；三端真实 Home 新增 6 项均通过 |
| 静态与工程守卫 | 四包 analyze 零问题；格式、ShellCheck、版本/文档、秘密扫描、核心摘要和平台边界检查通过 |
| 发布工具 | 定向 73 项通过，受信 CI artifact 的真实 ZIP 摘要和六文件布局核验通过 |
| 其他定向行为 | 订阅事务 81 项、Mac TUN DNS 隔离事务 25 项、960 节点关键路径烟测通过 |

两项首次失败发生于四包同时运行时，单独复测未重现；没有把超时上限放宽以换取通过。
共享全套加载于最后一处事件循环让出修复前，该小改已由最终控制器 15 项回归验证。
新增第 15 项用 100 个立即完成结果证明 UI 事件能在全部查询结束前取消批次。
最终远端 CI 将对完整提交再次执行各平台全部门禁，只有该结果通过才合并。

独立复审覆盖发布身份/ZIP 内容边界、后台缓存与三端接入。修复了审查发现的桌面自动恢复
保留意图代际时失败查询未重试的问题；重新连接仅清失败尝试，成功缓存不重新查询，测速暂停
恢复不重试此前失败。另补齐迁移线程职责后的守卫定位，订阅持久化文件上限精确调整至
184 行以容纳后台结果和取消收口，未增加持久化职责或删除校验。

本轮没有重新操作 Mac 正式客户端或 USB Android 手机，没有进行 Mac 断网测试。
Android 无 SIM、仅 Wi-Fi 的既定限制保持；Windows 新版交互实机验证仍由维护者在 Windows 主机执行。
自动化线程和后台调度证据不等同于实测耗电或帧率数据。旧版本实机结果及未覆盖项目继续保留在
[验收矩阵](../UAT_MATRIX.md)，不冒充 v4.0.36 全流程实机通过。

私家车成功延迟 24–39 ms 展示策略按项目明确要求保留；代理统计、账号用量卡边界、核心与
GeoIP 快照、Android 正式签名及受保护发布门禁均不放宽。

## 交付状态

候选代码尚在最终回归与独立审查阶段。只有受保护 PR 合并、精确 main 验证、三端 Release 构建
以及 GitHub/OSS 公开安装包和摘要核对通过后，才记录正式发布完成。
