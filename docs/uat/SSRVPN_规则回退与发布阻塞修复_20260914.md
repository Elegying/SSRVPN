# 规则回退与发布阻塞修复

日期：2026-09-14。对象为 `codex/independent-routing-updates` 当前本地未提交修改。本次承接《本地未提交发布审查》的两个阻塞项，保留原审查及失败日志，不将历史失败改写为通过。

## 修复

1. Windows 更新提示测试：完整等待循环放在同一个真实异步区域，避免真实文件/端口回调与模拟时间交替导致完成状态滞留。保留原 30 秒期限、提示内容断言和玻璃资源，不修改生产下载逻辑。Windows 包目录单独运行该测试文件：18 通过、1 项平台条件跳过。
2. 三端规则恢复：核心启动成功后记录实际使用版本；已有连接直接返回成功不确认未使用的新配置。只有已确认快照及其文件完整时才允许下载新版本。
3. 新规则明确加载失败时，完整停止旧启动流程，校验并启用上一成功快照，仅重试一次。桌面端必须确认系统代理/核心清理成功。普通权限、端口、TUN、认证、超时及取消不触发规则回退。
4. Android 回退同步应用名单、国内应用注释元数据及 provider 路径，保持 IPv6 边界、应用名单和手动规则原有顺序。回退读取当前配置中的应用规则，不依赖已损坏的新名单文件；节点凭据及 DNS 保持不变。
5. 持久记录失败版本上界，避免每次启动重复尝试相同坏版本。恢复提交中断后，下次准备连接继续修复。旧快照损坏时不启用；初始化保守基线仍保留。
6. 健康检查区分规则缺失与规则接口暂时不可用，并在非成功 HTTP 响应时取消响应流，释放资源。Android 原生使用独立规则错误类型，避免泛化为普通配置错误。

## 复审关注点

- 停止及取消跨越异步边界后，不能继续写配置或再次启动。
- 失败快照隔离先于恢复提交，持久清单、内存名单和连接配置一致后才重试。
- 恢复后的配置仍由真实内核加载，手动规则、GFW 和未知流量行为与恢复前约定一致。
- 磁贴沿用成功启动后保存的受保护配置；失败候选不保存为成功磁贴快照。
- 格式、代码规模和覆盖率门槛未降低；没有留下临时调试输出。

## 验证

定向回归 119 项通过，覆盖规则失败、候选文件损坏、旧文件损坏、取消、已运行误确认、只重试一次、重启恢复、应用名单顺序及规则接口恢复。真实 macOS 内核智能、全局、保守基线、恢复配置四组路由检查通过。四包静态分析、433 项发布工具测试和 25 项 macOS DNS 事务测试通过。

`scripts/verify-all.sh` 完整执行成功，退出码 0；末尾为 `All verification commands completed.`。修复后全量结果如下：

| 范围 | 测试结果 | 行覆盖率 |
| --- | --- | --- |
| 共享包 | 1,228 通过，1 项诊断条件跳过 | 88.27% |
| Android Flutter | 302 通过 | 69.99% |
| macOS Flutter | 326 通过 | 72.21% |
| Windows Flutter（macOS 主机） | 304 通过，7 项条件跳过 | 59.77% |

所有覆盖率门槛通过。Android 原生 39 个套件共 188 项测试，零失败、错误和跳过；macOS 原生 XCTest 及测试后崩溃/残留进程门禁成功。四组真实内核路由检查、格式和 ShellCheck、文档一致性、源代码基础凭据扫描及职责规模门禁通过。最后对补充的文档与修改执行独立收尾检查。

本轮两个原发布阻塞项已修复并通过复审，可以进入新候选版本构建与验收阶段；这不是已发布或实机全覆盖的声明。

## 证据边界

本次为本地源码修复和验证，没有提交、推送、合并或发布，也未构建或安装新的最终候选包。之前的预览包不包含本轮修复。Windows Flutter 测试在 macOS 主机运行，不能替代 Windows 原生构建、系统代理故障测试或实机验收。已通过测试不等于所有设备和网络零故障。

本轮日志保存于 `artifacts/rule-recovery-fix-20260914/`，目录已忽略，不进入源码提交。
