# SSRVPN 发布准备复核（2026-09-17）

## 结论

维护者决定本轮不做 Windows 实机测试。本地最终检查与 Mac/安卓构建已完成，未发现新的发布阻断代码问题，可以进入版本递增、提交和远端发布流程。Windows 实机覆盖缺口仍保留，不能称三端全部实机通过。

本轮沿用此前“不推送、不发布”的范围：没有提交、推送、打标签或触发公开发布。版本仍为本地候选 5.0.8+5008，不是新的公开版本。

## 最终源码及检查

基线提交 `712a6d02173628b1ba39545e116a07d991154102`，原有全部未提交修改保留。冻结文件哈希清单位于忽略目录 `artifacts/release-readiness-20260917/source-before.json`。

审查涵盖现有双栈规则生成、有限地址回退、会话隔离诊断计数、Windows 当前 TUN 恢复保护误报修正、手动规则输入，以及弹窗共享背景与玻璃几何分组。没有新增生产代码修改。额外文档检查发现调查报告的否定句被检查器判为过时表述，改为直接描述 IPv6 DIRECT 实测成功，未改变结论。

使用 Flutter 3.44.1 执行 `make verify`：

| 检查 | 结果 |
| --- | --- |
| 规则包、规则发布测试、版本同步、文档、格式和 ShellCheck | 通过 |
| 三端核心资产、源码契约、真实核心分流 | 通过 |
| 双栈转发及 SS/Trojan/AnyTLS 加密协议路径 | 通过 |
| Android 桥接、桌面启动、服务边界及其他静态守卫 | 通过 |
| macOS TUN DNS 事务 | 26/26 通过 |
| 发布工具测试 | 437 项通过；上传相关输出为模拟夹具，不是实际发布 |
| 性能冒烟、共享包及三端静态分析 | 通过；四组分析均无问题 |
| 共享 Dart 测试 | 1354 通过，1 跳过 |
| Android Dart 测试 | 341 通过 |
| Android 原生测试 | 191 通过，无失败/错误/跳过 |
| macOS Dart 测试 | 348 通过 |
| macOS 原生测试与结束后进程/崩溃检查 | 使用下述本地环境参数补跑通过 |
| Windows Dart 测试 | 323 通过，7 跳过；在 Mac 上运行，不是 Windows 实机测试 |
| 覆盖率门禁 | 全部通过 |

覆盖率：共享 89.59%，Android 70.98%，macOS 73.58%，Windows 60.86%；桌面关键文件覆盖率下限也通过。

### 原生检查环境例外

首次 `make verify` 在 macOS 原生测试处退出 65：Xcode 27 不再接受项目设置的 macOS 11 部署目标。采用本地临时 xcconfig `MACOSX_DEPLOYMENT_TARGET = 12.0` 补跑原生测试，再顺序完成剩余 macOS 覆盖率、Windows 测试及覆盖率步骤，退出码 0。

没有修改仓库最低系统支持设置；不能写成 `make verify` 一次完整全绿，也不能据此声称 macOS 11 实机通过。CoreSimulator 版本警告及旧 API 弃用警告未阻止本机 macOS 构建和原生测试。

原始本地日志：`/tmp/ssrvpn-final-verify-20260917.log`、`/tmp/ssrvpn-final-verify-resume-20260917.log`。不将全量测试日志作为公开发行附件。

## 构建和安装候选一致性

### macOS

从最终生产源码执行 `scripts/build-macos-local.sh`，保留项目使用量功能编译参数，并使用上述本地 xcconfig；release 构建成功，严格签名校验通过。

构建目录与 `/Applications/SSRVPN.app` 比较的 322 个实际文件全部相同，涵盖主程序、Flutter App/引擎、插件和 296 项 Flutter 资源。App.framework/App SHA256：

`b0342529810769f0bbc3b8ce7318ba6108cf06863064bc2bec7ea8cd7fd0e32e`

因此无需替换已安装候选，沿用此前相同候选的 Mac 实机证据。本轮未断网、未重连 TUN、未修改系统代理。

这证明已安装应用内容与此次本机构建一致；不等于读取了 root TUN 运行目录下的实际核心哈希，也不等于正式签名公证或旧版 macOS 兼容性验收。

### Android

使用现有预览配置构建 arm64 release APK，保留使用量功能编译参数。构建成功。USB 手机上的 `com.ssrvpn.android.preview` 为 5.0.8-preview / 5008。

通过 adb 回读已安装 base.apk，与本次构建 APK 完整 SHA256 相同，667 项 ZIP 内容无差异：

`30c773569c0a9856ce65f1c0930fa935cdca7b35e1842a64247b2d3fdf20ecbc`

无需重复覆盖安装或重复已做过的功能验收；未卸载、清除数据或替换正式包。Flutter 关于 KGP 的迁移提示没有阻止构建；项目使用内置 Kotlin 的守卫和原生测试均通过。

构建日志：`/tmp/ssrvpn-final-macos-build-20260917.log`、`/tmp/ssrvpn-final-android-build-20260917.log`。两端比对摘要存于 `artifacts/release-readiness-20260917/`。

## 实机证据与保留范围

本轮以重新构建及逐文件/完整 APK 一致性核验确认先前候选证据可沿用，遵守“不重复已做验收”。参见双栈增量实机验收和 macOS IPv6-only 归因报告。

- Mac/安卓既有连接、诊断及日志玻璃渲染的验收记录保留。
- 国内真实 IPv6 DIRECT 与普通代理 HTTPS 有实测证据。
- 当前节点没有 IPv6 出口，IPv6-only 代理成功不作为本轮可完成项；不得伪装转换或静默直连。
- 测试站证书故障与节点出口限制独立记录，证书验证保持有效。
- Windows 实机按维护者决定不做；长时间运行、低端设备、旧系统和未完成的手动 IPv6 规则实机输入保存，不新增通过结论。

## 进入公开发布时仍需执行

1. 同步递增三端版本号、构建号和发布说明，不能复用已公开的 5.0.8 身份。
2. 按项目流程提交/合并，确保发布标签指向最终受检提交。
3. 核验新核心资产可被干净 CI 环境取得；本地资产存在不证明远端镜像已发布。
4. 运行远端三端构建与发布门禁，核对正式产物、签名/校验和及下载结果。

这些是尚未执行的发布步骤，不是已经完成的公开发布证明。
