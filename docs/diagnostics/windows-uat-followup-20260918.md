# Windows 验收反馈处理（2026-09-18）

## 输入与归因

维护者提供的 [Windows 验收报告](../uat/SSRVPN_Windows_双栈启动用户验收_20260918.md) 针对 `00fba6c5a0c8cdba9fa19d0cbb25d0fedcb172cf`，不能冒充后续源码的实机结果。独立 evidence 目录未随附件提供。

报告确认三次首次 IP 查询成功（997/1270/1116 ms），TUN、系统代理及断开恢复正常。没有证据支持继续修改这些已通过的网络路径。

## 修复

1. **账号统计卡片间歇性消失**：控制器的失败和过期处理清空有效值，界面仅在有效值存在时创建两张卡片。保留严格过期、完整快照、单在途、退避与身份隔离；适用账号节点在等待/失败时保留两张占位卡，普通节点仍不显示账号卡。悬停/长按查看简明原因，既有运行日志仅记录失败类型变化和恢复。客户端区分超时、网络、证书、HTTP 拒绝和无效响应，不输出异常原文、URL、凭据或统计正文。尚不能确定用户当时远端查询失败的具体原因，不宣称网络层已被唯一归因。
2. **安装器模块路径污染**：Windows 安装、卸载初始化时，仅设置当前安装器进程的 `PSModulePath` 为其固定 Windows PowerShell 对应系统模块目录，所有助手进程继承；不修改用户/系统环境变量。设置失败在修改程序文件前中止，原有事务与状态报告保持。真实安装器 smoke 故意传入无系统模块的父进程路径，覆盖安装、升级、卸载；独立 PowerShell 清理夹具保留原环境。

## 回归

- 新增控制器失败、重复失败低噪音、恢复、身份清除测试，以及三尺寸占位布局测试；原有失败/过期不得展示旧数字的断言保留。
- 原界面测试要求失败时两张卡消失，已按本次修复目标改为验证占位和无旧数字；不是放宽数据有效性断言。
- HTTPS 测试增强为断言真实超时和证书拒绝的安全分类，继续验证凭据不跨重定向传播及响应上限。
- 本地账号统计专项 196 项通过，Windows 安装器配置检查 37 项通过。完整入口、Windows 实际安装及正式构建的最终结果以对应提交 Actions 记录为准。

## 未覆盖范围

用户报告没有测试 Windows 真实 IPv4-only 物理网络、抖音 UDP/QUIC、节点真实 IPv6 出口、正常托盘退出及新 UAC 授权。保持这些限制，不用自动化替代实机通过记录。本次补丁不改变核心二进制与转发逻辑。
