# 项目健康状态

最近审查：2026-07-14<br>
审查基线：基于 `v3.3.5` 的未发布本地修复，包含 Windows 无提权全新覆盖安装、主进程退出后原生代理兜底、Windows PowerShell 5.1 假绿防护、可重试的发布后资产校验，以及尚未推送或发版的发布事务、固定规则源、部分订阅结果、Android 单次恢复与端口提示证据。

## 结论

SSRVPN 已具备单一 Monorepo、三端发布链路、共享业务边界、失败回滚和完整本地门禁。当前主要短板不再是“基本功能缺失”，而是桌面签名、真实设备验收、无障碍证据与少数平台安全边界的继续收紧。

本文件使用固定 100 分量表，替代历史审查中互不兼容的 6.8/10、7.4/10、8.5/10 等结论。分数只代表当前仓库证据，不代表应用商店审核、第三方渗透测试或所有真实网络环境。

## 当前评分：90 / 100

| 维度 | 得分 | 当前证据 | 主要缺口 |
| --- | ---: | --- | --- |
| 正确性与恢复 | 19/20 | 连接代际、取消、Android 核心单次恢复、结构化部分订阅结果、系统代理所有权和更新回滚均有测试或守卫；Windows 主进程退出后还有启动器原生恢复与自有死端点禁用兜底 | 仍需扩大真实弱网和休眠/唤醒矩阵 |
| 安全与信任边界 | 18/20 | 外部输入有界、日志脱敏、资产摘要、Gitleaks 全历史扫描、固定提交的远程规则、Android/Windows 系统凭据保护、桌面 secret 验证迁移与崩溃一致性、Windows 每用户写入边界、macOS 最小文件权限、TUN 临时授权边界 | macOS/Windows 未受信任签名；桌面运行时配置仍有同用户可读残余风险 |
| 用户体验与状态可信度 | 17/20 | 首次导入、连接取消、订阅部分失败警告、端口临时调整提示、Android 恢复通知、桌面代理恢复、受认证的核心健康检查，以及 Windows 安装全新覆盖与死代理自动解除 | Windows 安装升级不保留用户配置；无障碍和多语言仍缺少真机证据 |
| 可维护性与自动验证 | 19/20 | workspace analyze、四套 Flutter 测试、Android 原生测试、发布工具测试、边界脚本和覆盖率门槛均纳入 `make verify`；Windows CI/Release 逐次传播 PowerShell 子进程失败，并以真实 5.1 解析全部脚本 | 大型桌面 UI/服务文件仍需按行为切片，不能为重构而重构 |
| 发布与文档 | 17/20 | 版本/资产/校验和/来源守卫、OSS 先推广且可补偿回滚的 GitHub 最终公开事务、Windows 全新覆盖安装守卫、安装与便携包流程及数据清除警告 | 真实 Windows 安装升级矩阵与付费平台签名尚未完成 |

评分调整规则：新增用户可见行为若没有回归测试，不能提升正确性或 UX 分；覆盖率提升若只靠排除源码，不能提升自动验证分；签名、公证和真机结果只有在产生可复核证据后才计分。

## 验证状态

根门禁：

```bash
make verify
```

它覆盖版本与包内指南、核心资产、Android bridge、解锁取消、桌面启动、Clash 边界、macOS 特权边界、Windows launcher、PowerShell 5.1/CI 退出码守卫、密钥扫描、发布工具、workspace analyze、四套 Flutter 测试、Android 原生测试和覆盖率。

当前覆盖率门槛已从 `50/40/10/12` 收紧为：

| 目标 | 最近测量 | 门槛 |
| --- | ---: | ---: |
| `ssrvpn_shared` | 72.12% | 65% |
| Android | 58.71% | 50% |
| macOS | 47.53% | 30% |
| Windows | 41.06% | 30% |

2026-07-14 最近一次完整本地运行通过共享 273 项、Android Flutter 100 项、macOS
73 项、Windows 64 项和发布工具 58 项；Windows 的真实 DPAPI/`MoveFileExW` 及 Mihomo
集成测试因当前主机是 macOS 按条件跳过，已保留在 Windows CI 中执行。Android 原生
JUnit/Gradle 门禁同时通过。测量值会随可执行行变化；是否健康以同一提交上的
`make verify` 输出为准，不手工追求百分比。

## 平台状态

| 平台 | 状态 | 说明 |
| --- | --- | --- |
| Shared | 健康 | 订阅、配置、更新、脱敏、解锁检测、桌面控制器、窗口状态和启动日志公共逻辑由共享包承载，并启用 Flutter 推荐 lint |
| Android | 健康，需真机回归 | VPN Service、快捷磁贴和通知有原生边界；健康检查要求受认证 Mihomo API 返回有效 HTTP 响应，运行中异常退出最多恢复一次并明确通知，用户断开可取消恢复 |
| macOS | 代理与 TUN 可用，分发信任受限 | 系统代理恢复失败时保留核心供再次断开或下次连接前恢复；TUN 每次连接请求管理员授权，并使用摘要校验、临时 root 目录、应用 PID 监视和退出清理；包仍是 ad-hoc、未公证 |
| Windows | 代码与 CI 健康，需 Windows 验收 | 每用户安装版与便携版并存；Windows runner 会用 PowerShell 5.1 阻断脚本与打包失败，但仍不能替代安装、升级、异常退出、重启和代理恢复真机测试 |

## 继续提升到 92 分的工作

1. 在干净 Windows 11 虚拟机完成安装版与便携版的首次启动、连接、断开、退出、全新覆盖升级、损坏目录覆盖和卸载矩阵，并保存结果。
2. 为 macOS 配置 Developer ID 与 notarization，随后把专用 secret 文件验证迁移到 Keychain；为 Windows 配置 Authenticode。若继续公开分发 macOS TUN，审计并收窄特权 helper/Network Extension 架构。
3. 用 TalkBack、VoiceOver 和 Windows Narrator 验证连接主流程、订阅导入、错误恢复和动态状态播报，补齐键盘与焦点回归测试。
4. 将桌面和 Android 的启动/连接错误映射为统一、可操作且可脱敏的错误类别，并让故障指南与 UI 文案共享同一语义。
5. 按用户行为拆分仍然过大的 UI/服务热点，同时保持每次切片可独立测试和回滚。

## 更新规则

每次发布只记录已验证事实：版本、提交、命令、平台、产物和残余风险。历史审查保留作证据，但不得覆盖本文件、[安全策略](../SECURITY.md)、[测试策略](TESTING.md) 与 [文档索引](README.md) 的当前结论。
